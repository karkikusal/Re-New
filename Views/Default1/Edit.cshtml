@model WebApplication2.Models.employee_salary_detail
@{
    ViewBag.Title = "Edit";
}


<h2>Edit</h2>

@using (Html.BeginForm("updateData", "Default1"))
{
    <div class="form-horizontal">
        <input type="hidden" name="id" value="@Model.id " />
        <div class="form-group">
            <label>Employee Name</label>
            @*@<select>
                    @foreach(var data in ViewBag.empsList)
                        {
                            <option>(data.name)</option>
                        }
                </select>*@

            @Html.DropDownList("employeeList", null, "Select", htmlAttributes: new { @Name = "employee_id" })
            @*@<input type="text" name="employee_id" class="form-control" />*@
        </div>
        <div class="form-group">
            <label>Salary Paid</label>
            <input type="text" name="salary_paid" class="form-control" />
        </div>
        <div class="form-group">
            <label>Paid Date</label>
            <input type="date" name="paid_date" class="form-control" />
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" class="btn btn-success" value="Save" />
            </div>
        </div>
    </div>
}


